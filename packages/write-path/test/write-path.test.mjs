import { describe,it,expect } from 'vitest'; import { createRequire } from 'node:module'; const {proposeChange,confirmChange,rejectChange}=createRequire(import.meta.url)('../src');
const base=()=>({stages:[{id:'a',name:'A',status:'in_progress'},{id:'b',name:'B',status:'not_started'}],current_stage_index:0,overall_completion_pct:0,pending_confirmation:null,status_dots:[],snapshots:[]});
describe('write path',()=>{it('allows only one pending change',()=>{const a=proposeChange(base(),{targetStageId:'a',targetStatus:'complete',summary:'done',requestedBy:'test'});expect(()=>proposeChange(a,{targetStageId:'b',targetStatus:'in_progress',summary:'next',requestedBy:'test'})).toThrow()});it('confirms immutably and advances',()=>{const original=base();const pending=proposeChange(original,{targetStageId:'a',targetStatus:'complete',summary:'done',requestedBy:'test'});const next=confirmChange(pending);expect(original.pending_confirmation).toBeNull();expect(next.stages[0].status).toBe('complete');expect(next.current_stage_index).toBe(1);expect(next.overall_completion_pct).toBe(50);expect(next.pending_confirmation).toBeNull()});it('rejects to a status dot',()=>{const next=rejectChange(proposeChange(base(),{targetStageId:'a',targetStatus:'complete',summary:'no',requestedBy:'test'}),'because');expect(next.status_dots).toHaveLength(1);expect(next.pending_confirmation).toBeNull()})});
